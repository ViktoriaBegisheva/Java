package numbers;

public interface Number {
	NewNumber add(NewNumber other);
	NewNumber subtract(NewNumber other);
	NewNumber multiply(NewNumber other);
	NewNumber divide(NewNumber other);

}
